from resources.functions.candlestick_utils import TD

VALID_CASES = [tuple(x) for x in TD["valid_cases"]]
TIME_RANGE_CASES = [tuple(x) for x in TD["time_range_cases"]]
NEGATIVES = TD["negatives"]
LIMITS = TD["limits"]
